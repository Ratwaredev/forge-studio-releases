name: Legacy Forge 0.34 bootstrap (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical release path
        run: |
          echo "This Forge 0.34 bootstrap is intentionally disabled."
          echo "It previously dispatched the legacy Ratlab-based controlled-release workflow."
          echo "Use release-current-forge.yml or build-forge.yml for current Forge releases."
          echo "No workflow was dispatched and no release was created."
