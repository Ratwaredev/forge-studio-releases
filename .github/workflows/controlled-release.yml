name: Legacy controlled Forge release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical release path
        run: |
          echo "This legacy workflow is intentionally disabled."
          echo "It previously cloned Ratwaredev/ratlab and could publish those artifacts as Forge Studio."
          echo "Forge source is now canonical in Ratwaredev/forgestudio."
          echo "Use release-current-forge.yml or build-forge.yml for the verified clean-source release path."
          echo "No build or release was created."
