name: Legacy Forge source-current path (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical release path
        run: |
          echo "source-current is a legacy branch and no longer contains Forge application source."
          echo "Use Build and publish Forge Studio (build-forge.yml), which consumes the verified Ratwaredev/forgestudio release-source delivery."
          echo "No release was created."
