name: Legacy Forge 0.35 source migration (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical source path
        run: |
          echo "This Forge 0.35 migration bridge is intentionally disabled."
          echo "It previously cloned historical Forge code from Ratwaredev/ratlab and published a temporary encrypted bootstrap asset."
          echo "Forge source is now canonical in Ratwaredev/forgestudio and delivered through the verified release-source pipeline."
          echo "No source migration, asset upload, tag creation, build, or release was performed."
