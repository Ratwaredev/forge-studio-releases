# Forge Studio Releases

Public installers and automatic-update metadata for Forge Studio.

The application source code remains in a separate private repository. This repository contains only release binaries, blockmaps, updater metadata, checksums, and SBOM files.
